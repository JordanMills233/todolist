import React, { useState } from "react";
import styles from "../styles/App.module.css";

const App: React.FC = () => {
  return (
    <div className={styles.mainContainer}>
      <h1 className={styles.tester}>testing</h1>
    </div>
  );
};

export default App;
